{
  "name": "lol-companion",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/sync-ddragon.js && vite build",
    "preview": "vite preview",
    "sync": "node scripts/sync-ddragon.js"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.3"
  }
}
